import Scrollbar from 'react-scrollbars-custom'
import styled from 'styled-components'

export const StyledScrollbar = styled(Scrollbar)`
  & .ScrollbarsCustom-Thumb {
    background: var(--ant-color-text-placeholder) !important;
  }
`
